{
  "name": "ts-print-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w ts-print",
    "test": "npm run build && npm run build -w test-target && npm run test -w tests",
    "eslint": "eslint",
    "eslint:fix": "eslint --fix"
  }
}
