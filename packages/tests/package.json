{
  "name": "tests",
  "version": "0.1.0",
  "private": "true",
  "scripts": {
    "test": "vitest run",
    "prettier": "prettier src --check",
    "prettier:fix": "prettier src --write"

  },
  "devDependencies": {
    "vitest": "^4.0.6"
  }
}
